const router = require("express").Router();
const mongoose = require("mongoose");
const Cafe = require("../models/Review.model.js");

const {isAuthenticated} = require ("./middleware/jwt.middleware.js");

//POST

//READ


module.exports = router;